# Desktop
.DS_Store

# IDE — local history / editor state
.lh
.history
.idea
.vscode

# npm
npm-debug.log
node_modules/*
package-lock.json

# Test artifacts
tests/browser/swig.pack.js
tests/browser/*.test.js
lib-cov/*
tests/coverage.html

# Build output (regenerated by `make build` / `make build-docs`)
dist/*
tmp
tmp_build/*
docs/css/*
docs/docs/*.json
docs/coverage.html
browser/test/tests.js
tests/browser.js

# Claude AI context (local only — mirror Gina's convention)
# Use `git add -f <path>` to force-track a specific file if needed.
CLAUDE.md
.claude/

# Desktop
.DS_Store

# IDE — local history / editor state
.lh
.history
.idea
.vscode
.jshintrc

# Local tool / automation artifacts (debug logs, MCP session output)
.playwright-mcp/

# CI / GitHub metadata
.github/

# Workspaces — sibling packages publish independently
packages/**

# Sources not needed in the published tarball
# (browser/ source → shipped as dist/*.js; docs/ and examples/ are site/demos only)
# (.changes/ → changie fragments; the rendered HISTORY.md ships instead)
.changes/
*.html
benchmarks/**
browser/**
docs/**
examples/**
Makefile
scripts/**
tests/**
.travis.yml
CONTRIBUTING.md

# AI / internal development notes (local only — mirror Gina's convention)
.claude/
CLAUDE.md
